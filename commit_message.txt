Ramin Yazdani | PCA and Missing Data Imputation | main | WIP(setup): Add .gitignore for Python and Jupyter artifacts

Add .gitignore to exclude Python cache files, Jupyter checkpoints, virtual 
environments, and IDE configuration files. This ensures the repository stays 
clean and only tracks source code and essential files.

Contents added:
- .gitignore (Python, Jupyter, IDE, OS file patterns)

Patterns covered:
- Python bytecode (__pycache__/, *.pyc, *.pyo)
- Jupyter checkpoints (.ipynb_checkpoints/)
- Virtual environments (venv/, env/, .venv/)
- IDE files (.vscode/, .idea/, *.swp)
- OS files (.DS_Store, Thumbs.db)

Verified:
- .gitignore rules will exclude common development artifacts
- Pattern syntax is correct for Git
